# Local DB/storage stack config
# DB_HOST_BIND controls database/admin panel ports.
# Use 127.0.0.1 first. Later change to your LAN IP, e.g. 192.168.1.126, after firewall rules.
DB_HOST_BIND=127.0.0.1

# Static dashboard page.
# Keep this on localhost if Caddy will proxy https://db.home.arpa to 127.0.0.1:8080.
DASHBOARD_HOST_BIND=127.0.0.1
DASHBOARD_PORT=8080

# PostgreSQL + pgvector
POSTGRES_PORT=5432
POSTGRES_DB=langgraph_app
POSTGRES_USER=langgraph_user
POSTGRES_PASSWORD=change_me_postgres_2026

# pgAdmin
PGADMIN_PORT=5050
PGADMIN_DEFAULT_EMAIL=admin@local.dev
PGADMIN_DEFAULT_PASSWORD=change_me_pgadmin_2026

# Redis
REDIS_PORT=6379
REDIS_PASSWORD=change_me_redis_2026

# RedisInsight
REDISINSIGHT_PORT=5540

# Neo4j
NEO4J_HTTP_PORT=7474
NEO4J_BOLT_PORT=7687
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=change_me_neo4j_2026
NEO4J_HEAP_INITIAL=512m
NEO4J_HEAP_MAX=1G
NEO4J_PAGECACHE=512m

# MinIO
MINIO_API_PORT=9000
MINIO_CONSOLE_PORT=9001
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=change_me_minio_2026
MINIO_DEFAULT_BUCKET=langgraph-app
